# Skill Creation Brief

**Requested by:** Skiller (meta-agent)
**Date:** [ISO date]
**For task:** [task-NNN] — [task-name]

---

## What This Skill Must Do

**Core capability:** [Imperative sentence. E.g., "Synthesize two or more markdown documents into a unified framework document that preserves key arguments from all sources."]

**Trigger phrases** (what a user would say to invoke this skill):
- "[phrase 1]"
- "[phrase 2]"
- "[phrase 3]"

**Does NOT handle:** [Explicit exclusions. E.g., "Does not handle PDF input, does not produce slides, does not perform web research."]

---

## Inputs This Skill Will Receive

| Input | Type | Description |
|-------|------|-------------|
| [input-1] | [file path / text / JSON] | [what it contains] |
| [input-2] | [file path / text / JSON] | [what it contains] |

**Typical invocation context:** [One sentence. E.g., "Called by Skiller during task execution; inputs are markdown files in the wip/ folder of the current working directory."]

---

## Required Output

**Output artifact:** [filename and extension, e.g., `synthesis.md`]
**Output format:** [markdown | JSON | plain text | code]
**Output location:** [e.g., "write to wip/task-NNN-output.md"]

**Success looks like:**
- [Criterion 1 — concrete and verifiable]
- [Criterion 2 — concrete and verifiable]
- [Criterion 3 — concrete and verifiable]

---

## Constraints and Rules

- [Constraint 1. E.g., "Output must be under 2000 words."]
- [Constraint 2. E.g., "Must preserve source attribution for each synthesized point."]
- [Constraint 3. E.g., "Must not add opinions or content not present in the input documents."]

---

## Example

**Example input** (first few lines only):
```
[Brief illustration of what a typical input looks like]
```

**Example output** (first few lines only):
```
[Brief illustration of what the first lines of output should look like]
```

---

## Quality Gate

Before this skill is considered complete it must:
- [ ] Produce output meeting all three success criteria when tested on the example input
- [ ] Have a SKILL.md description that accurately describes when to use it
- [ ] Require no inputs beyond the Inputs table above

---

## Notes to Skill Creator

[Any additional context that will improve the created skill. Note whether this skill should be general-purpose (reusable across future Skiller sessions) or narrowly scoped to this one job. The example input is illustrative; actual inputs will vary.]
